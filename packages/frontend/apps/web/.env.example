# ─── FlowState Frontend ───────────────────────────────────────────────────────
# Public URL of your Hono API (apps/api) — accessible from the browser
NEXT_PUBLIC_API_URL=https://your-api.railway.app

# Coach workspace ID — the Affine workspace routed to the admin dashboard
# Default: coach-admin  (change only if you create a different workspace ID)
VITE_COACH_WORKSPACE_ID=coach-admin
